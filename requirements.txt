# CARN-X — runtime dependencies (Streamlit Community Cloud reads this file)
# ---------------------------------------------------------------------------
# CPU-only PyTorch: the --extra-index-url keeps the wheel small so the cloud
# build stays inside its time / disk budget.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2  # resolves to the small CPU wheel from the index above

# scientific stack
numpy
pandas>=2.2
scipy
statsmodels
scikit-learn
arch
ruptures
pywavelets
sympy
networkx

# app + data
# pinned exactly: scroll_boot's nav wheel uses the deprecated (but still
# functional) st.components.v1.html to reach the real page from an iframe --
# verified end-to-end against this exact version. An unpinned `>=` let Cloud
# silently resolve a newer release that could drop the API without warning.
streamlit==1.60.0
altair
yfinance
pyarrow
